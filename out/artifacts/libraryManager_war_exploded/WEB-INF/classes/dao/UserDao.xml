<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.UserDao">
    <select id="findUserByUserId" parameterType="string" resultType="user">
        SELECT user_id as userId, name, email, password, permissions
        FROM user
        WHERE user_id = #{userId}
    </select>

    <!-- 添加用户 -->
    <insert id="addUser" parameterType="registerDto">
        INSERT INTO user (user_id, name, email, password, permissions)
        VALUES (#{userId}, #{name}, #{email}, #{password}, #{permissions})
    </insert>

    <!-- 根据邮箱查询用户 -->
    <select id="findUserByEmail" parameterType="string" resultType="user">
        SELECT user_id as userId, name, email, password, permissions
        FROM user
        WHERE email = #{email}
    </select>

    <!-- 根据邮箱修改用户密码 -->
    <update id="updatePasswordByEmail">
        UPDATE user
        SET password = #{password}
        WHERE email = #{email}
    </update>

    <insert id="addAdministrator" parameterType="user">
       INSERT INTO user(user_id, name, password, permissions)
       VALUES (#{userId}, #{name}, #{password}, #{permissions})
    </insert>

</mapper>